# Strapi API (server-side only — no NEXT_PUBLIC_ prefix)
STRAPI_URL=http://localhost:1337
STRAPI_API_TOKEN=

# Site URL (public — used in metadata, sitemap, canonical)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Strapi webhook revalidation secret (server-side)
REVALIDATE_SECRET=
